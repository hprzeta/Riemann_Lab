# ===== SOCLE COMMUN — synchronise sur les 4 branches (hprzeta) =====
# Environnement virtuel
zeta_env/
__pycache__/
__init__.py
*.pyc

# Donnees volumineuses & logs
# /mnt/data/
*.log

# IDE
.vscode/
.spyder-py3/

# Fichiers temporaires
*.tmp
*.bak
.DS_Store

# Notebooks
.ipynb_checkpoints/

# Clone du wiki (depot separe — ne pas suivre depuis le repo principal)
Riemann_Lab.wiki

# IA — Claude Code : ignorer .claude/ SAUF les skills (versionnes en Phase 2)
.claude/*
!.claude/skills/
!.claude/skills/**
claude-traitement-journalier/

# Audit systeme local
scripts/hprzeta_system_audit_*.tar.gz
scripts/hprzeta_system_audit_*.zip
scripts/assistant_pack_audit_*.txt
scripts/audit_extract_text*/

# SECRETS — ne jamais committer
.mcp.json
.env
.env.local

# Securite supplementaire : dossier local de secrets (~/riemann_handoff/secrets_local/,
# hors repo par defaut) — regle de garde au cas ou il serait un jour copie/imbrique
# par erreur dans l'arbre du projet. Contient cle WireGuard, mots de passe fibre, IPv6
# publiques, zips personnels non tries — jamais destine a etre versionne.
secrets_local/
**/secrets_local/

# Captures d'écran WireGuard/VPN centralisées — local uniquement (IPv6 publiques,
# cle publique WireGuard, sorties wg show/tcpdump — pas versionne sans accord explicite)
docs/archive/wireguard_screenshots/

# Backups contenant des secrets (cle privee WireGuard PC4, etc.) — jamais versionnes
docs/archive/zeta-secure-backup.tgz
docs/archive/*-backup.tgz

# Archive box SFR — mot de passe fibre en clair, MAC, IPv6 publiques — jamais versionne
docs/archive/BOX-SFR-*.pdf

# Contient la cle privee WireGuard du peer telephone (10.10.0.3, confirme 05/09/2026) — jamais versionne
docs/archive/Pass-20260819T194523Z-1-001.zip

# Etat de session volatil — local uniquement
Handoff.md
**/Handoff.md

# Notes personnelles locales — non versionnees
materielunixlitepc2.txt
pcemmaus.txt
# ===== FIN SOCLE COMMUN =====
# (motifs specifiques a une branche, ex. *.so *.o pour C, a ajouter SOUS cette ligne)

# Staging PDF → MD (locaux, non versionnés)
pdf/optimisation/Voie_b_5/.pdf_to_md/
pdf/optimisation/Voie_b_5/md_to_claude/
# Résultats de run (CSV, PNG, logs) — dossiers de sortie des scripts
# NB : la règle nue `calculs/` ignorait aussi src/calculs/ (module Python) →
#      corrigé : règles précises ci-dessous pour ne pas bloquer les nouveaux modules.
/calculs/
src/calculs/optimisation/calculs/
test-results/
logs/*.pid
pdf/optimisation/*.aux
pdf/optimisation/*.out
